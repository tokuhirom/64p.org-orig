opensolaris

* 感想
- perl が 5.8.4
- 自分のIPみるには ifconfig -a
- アカウントが 8 文字まで

* フリーソフトのインスコ
http://sy2920.s151.xrea.com/solaris/pkg-get.html

sudo pkg install gcc-dev
sudo pkg install SUNWgit
sudo pkg install SUNWgnu-coreutils
sudo pkg install SUNWzsh
sudo pkg install SUNWscp
sudo pkg install SUNWtree
あたりは必須か(svn はデフォルトではいってる)。SUNWscp の中に whereis とかはいっ
てる。

sudo pfexec pkg install SUNWipkg
で、pkg(5) コマンド自体をアップグレードできる。

sudo pkg image-update

* X なしで起動するには
pfexec svcadm disable gdm

* ssh につなぐ

以下のコマンドを実行する。
VBoxManage setextradata "vmname" "VBoxInternal/Devices/pcnet/0/LUN#0/Config/guestssh/Protocol" TCP
VBoxManage setextradata "vmname" "VBoxInternal/Devices/pcnet/0/LUN#0/Config/guestssh/GuestPort" 22
VBoxManage setextradata "vmname" "VBoxInternal/Devices/pcnet/0/LUN#0/Config/guestssh/HostPort" 2222

もし以下のようなエラーがでたら、
Configuration error: Failed to get the "MAC" value (VERR_CFGM_VALUE_NOT_FOUND).
Unknown error creating VM (VERR_CFGM_VALUE_NOT_FOUND).
ネットワークインターフェースをPCNet-Fast III にする。

ssh -p 2222 tkhrm@127.0.0.1 でつなげる。

* perl
いつもどおりに Bundle::CPAN をアップグレードしておく。

* いらないものを消す

646 とかはいってるけど、いらないのを消すよろし。

sudo pkg uninstall -r SUNWfirefox
sudo pkg uninstall -r SUNWthunderbird
sudo pkg uninstall -r SUNWevolution SUNWevolution-libs

* locate がない

slocate(secure locate) があるので、そっちをつかう。

* chsh
pfexec passwd -e
でできる。

* guest tools を入れる
guest tools(vmware tools みたいなやつ)をいれないと時刻がずれるので必須。

guest tools をマウントして、以下のコマンドをたたく。
pkgadd -d /media/VBOXADDITIONS_1.6.2_31466/VBoxSolarisAdditions.pkg

* Solaris の利点

kazuho さん曰く。
- zone
- l2arc
- container

